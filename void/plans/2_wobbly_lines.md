# ПЛАН РЕАЛИЗАЦИИ: Wobbly / Jittery Lines (Дрожащие линии)

## 🎯 Концепция

Добавить контролируемое "дрожание" к контурам модулей, создавая эффект 
рукотворного, живого рисунка. Каждая точка контура смещается на небольшую 
случайную величину, управляемую Perlin/Simplex noise для плавных переходов.

---

## 📐 Архитектура

### Новые файлы:
```
js/
├── utils/
│   └── NoiseGenerator.js    // Генератор Perlin/Simplex noise
├── effects/
│   └── WobblyEffect.js      // Применение эффекта к контурам
```

### Изменяемые файлы:
- js/core/ModuleDrawer.js    // Интеграция эффекта при рендеринге
- js/core/VoidExporter.js    // Поддержка экспорта в SVG
- js/main.js                 // UI-контролы
- index.html                 // Слайдеры параметров
- styles.css                 // Стили для новых контролов

---

## 🔧 Этап 1: Генератор шума (NoiseGenerator.js)

### 1.1 Simplex Noise

Использовать Simplex noise вместо Perlin — он быстрее и не имеет 
артефактов направленности.

Можно:
a) Написать свою реализацию (~100 строк)
b) Портировать из открытых библиотек (simplex-noise.js)
c) Использовать простой псевдослучайный генератор с интерполяцией

### 1.2 API генератора

```javascript
class NoiseGenerator {
    constructor(seed = Math.random()) {
        this.seed = seed;
        this.permutation = this.generatePermutation(seed);
    }
    
    // 2D noise: возвращает значение от -1 до 1
    noise2D(x, y) { ... }
    
    // Получить смещение для точки
    getOffset(x, y, frequency, amplitude) {
        return {
            dx: this.noise2D(x * frequency, y * frequency) * amplitude,
            dy: this.noise2D(x * frequency + 100, y * frequency + 100) * amplitude
        };
    }
    
    // Обновить seed (для кнопки Update)
    reseed(newSeed) { ... }
}
```

### 1.3 Параметры шума

| Параметр | Описание | Диапазон |
|----------|----------|----------|
| frequency | Частота шума (мельче/крупнее) | 0.01 - 0.5 |
| amplitude | Амплитуда смещения (в пикселях) | 0 - 20 px |
| seed | Зерно генератора | случайное |

---

## 🔧 Этап 2: Эффект дрожания (WobblyEffect.js)

### 2.1 Применение к линии

```javascript
class WobblyEffect {
    constructor(noiseGenerator) {
        this.noise = noiseGenerator;
    }
    
    // Применить эффект к массиву точек
    applyToPath(points, options) {
        const { frequency, amplitude, detail } = options;
        
        // 1. Увеличить количество точек для детализации
        const densifiedPoints = this.densifyPath(points, detail);
        
        // 2. Сместить каждую точку по noise
        return densifiedPoints.map((pt, i) => {
            const offset = this.noise.getOffset(pt.x, pt.y, frequency, amplitude);
            return {
                x: pt.x + offset.dx,
                y: pt.y + offset.dy
            };
        });
    }
    
    // Добавить промежуточные точки между существующими
    densifyPath(points, segmentsPerUnit) {
        const result = [];
        for (let i = 0; i < points.length - 1; i++) {
            const p1 = points[i];
            const p2 = points[i + 1];
            const dist = Math.hypot(p2.x - p1.x, p2.y - p1.y);
            const segments = Math.max(1, Math.floor(dist / segmentsPerUnit));
            
            for (let j = 0; j <= segments; j++) {
                const t = j / segments;
                result.push({
                    x: p1.x + (p2.x - p1.x) * t,
                    y: p1.y + (p2.y - p1.y) * t
                });
            }
        }
        return result;
    }
}
```

### 2.2 Типы контуров для обработки

| Тип модуля | Контуры |
|------------|---------|
| straight | 2 параллельные линии |
| round | 2 дуги 90° |
| bend | 2 дуги 90° (разного радиуса) |
| joint | 4 линии от центра |
| link | 2 короткие линии |
| central | крестовина |

Для каждого типа модуля нужно:
1. Получить контур как массив точек
2. Применить WobblyEffect
3. Отрисовать результат

---

## 🔧 Этап 3: Интеграция с ModuleDrawer

### 3.1 Режим работы

Wobbly может работать как:
a) Отдельный режим (mode: 'wobbly')
b) Модификатор для других режимов (toggle)

**Рекомендую вариант (b)** — toggle, который можно включить для любого режима.

### 3.2 Изменения в ModuleDrawer

```javascript
// В методе рендеринга модуля
renderModule(type, params) {
    // Получить базовые контуры модуля
    let contours = this.getModuleContours(type, params);
    
    // Применить wobbly эффект, если включен
    if (params.wobbly && params.wobblyAmount > 0) {
        contours = contours.map(contour => 
            this.wobblyEffect.applyToPath(contour, {
                frequency: params.wobblyFrequency,
                amplitude: params.wobblyAmount,
                detail: params.wobblyDetail
            })
        );
    }
    
    // Отрисовать контуры
    this.drawContours(contours, params);
}
```

### 3.3 Новый метод: getModuleContours

Нужно создать метод, возвращающий контуры модуля как массив точек:

```javascript
getModuleContours(type, params) {
    switch(type) {
        case 'straight':
            return this.getStraightContours(params);
        case 'round':
            return this.getRoundContours(params);
        // ... и т.д.
    }
}
```

---

## 🔧 Этап 4: UI-контролы

### 4.1 HTML (добавить в index.html)

```html
<!-- Wobbly Effect Controls -->
<div class="control-group toggle-group-row" id="wobblyControlGroup" style="display: none;">
    <label class="checkbox-label" for="wobblyCheckbox">
        <input type="checkbox" id="wobblyCheckbox">
        <span>Wobbly</span>
    </label>
</div>

<div class="control-group" id="wobblyAmountControlGroup" style="display: none;">
    <label for="wobblyAmountSlider">
        <span>Wobble Amount</span>
        <input type="text" class="value-display" id="wobblyAmountValue" value="3">
    </label>
    <input type="range" id="wobblyAmountSlider" min="0" max="20" value="3" step="0.5">
</div>

<div class="control-group" id="wobblyFrequencyControlGroup" style="display: none;">
    <label for="wobblyFrequencySlider">
        <span>Wobble Scale</span>
        <input type="text" class="value-display" id="wobblyFrequencyValue" value="0.1">
    </label>
    <input type="range" id="wobblyFrequencySlider" min="0.01" max="0.5" value="0.1" step="0.01">
</div>
```

### 4.2 Параметры

| UI-название | Внутреннее имя | Описание |
|-------------|----------------|----------|
| Wobbly | wobblyEnabled | Toggle включения |
| Wobble Amount | wobblyAmount | Сила дрожания (px) |
| Wobble Scale | wobblyFrequency | Масштаб шума |

### 4.3 Видимость контролов

Показывать Wobbly toggle во всех режимах кроме Random.
Показывать слайдеры Amount и Scale только когда Wobbly включен.

---

## 🔧 Этап 5: Экспорт в SVG (VoidExporter.js)

### 5.1 Формат SVG path

Wobbly-контуры экспортируются как обычные path с множеством точек:

```svg
<path d="M x0,y0 L x1,y1 L x2,y2 ... L xn,yn Z" 
      fill="none" stroke="currentColor" stroke-width="..."/>
```

Или с Bezier-сглаживанием для меньшего размера файла:

```svg
<path d="M x0,y0 Q cx,cy x1,y1 Q cx,cy x2,y2 ... Z" />
```

### 5.2 Оптимизация

- Округлять координаты до 1-2 знаков после запятой
- Использовать относительные команды (l вместо L)
- При экспорте использовать тот же seed, что и при рендеринге

---

## 📋 Порядок реализации

### Фаза 1: Базовый noise (1-2 часа)
1. ✅ Создать NoiseGenerator.js
2. ✅ Реализовать Simplex noise 2D
3. ✅ Тестирование на простых примерах

### Фаза 2: Эффект дрожания (2-3 часа)
1. ✅ Создать WobblyEffect.js
2. ✅ Реализовать densifyPath
3. ✅ Реализовать applyToPath
4. ✅ Тестирование на массивах точек

### Фаза 3: Интеграция с рендерингом (3-4 часа)
1. ✅ Добавить getModuleContours для каждого типа модуля
2. ✅ Интегрировать WobblyEffect в ModuleDrawer
3. ✅ Тестирование рендеринга

### Фаза 4: UI (1-2 часа)
1. ✅ Добавить HTML-контролы
2. ✅ Добавить обработчики в main.js
3. ✅ Добавить настройки в Settings

### Фаза 5: SVG экспорт (1-2 часа)
1. ✅ Интегрировать в VoidExporter
2. ✅ Тестирование экспорта
3. ✅ Оптимизация размера файла

---

## ⚠️ Потенциальные проблемы и решения

| Проблема | Решение |
|----------|---------|
| Разные результаты при перерисовке | Сохранять seed, использовать кнопку Update для нового |
| Медленный рендеринг | Уменьшить detail (меньше точек) |
| Слишком резкие переходы | Увеличить frequency (более плавный шум) |
| Контуры пересекаются | Ограничить amplitude относительно stem |
| Большой размер SVG | Использовать Bezier-аппроксимацию |

---

## 🎨 Ожидаемый результат

- Буквы будут выглядеть как нарисованные от руки
- Контролируемая степень "небрежности"
- Каждое обновление даёт новый уникальный результат
- Работает со всеми режимами (Mono, Poly, Dash и т.д.)

---

## 📊 Оценка сложности

| Аспект | Оценка |
|--------|--------|
| Сложность реализации | ⭐⭐ (низкая) |
| Влияние на производительность | ⭐ (минимальное) |
| Сложность SVG-экспорта | ⭐ (простой) |
| Визуальный эффект | ⭐⭐⭐⭐ (сильный) |

**Общее время реализации: 8-13 часов**
