# План реализации Metaballs для Void Typeface

## 🎯 Концепция

Metaballs — это техника, где несколько "капель" (источников потенциала) создают единое поле. Там, где капли близко друг к другу, они "сливаются". Мы используем алгоритм **Marching Squares** для извлечения контура из этого поля.

---

## 📐 Архитектура

### Новые файлы:
```
js/
├── core/
│   ├── MetaballField.js      // Вычисление поля потенциалов
│   ├── MarchingSquares.js    // Алгоритм извлечения контуров
│   └── MetaballRenderer.js   // Рендеринг и интеграция
```

---

## 🔧 Этап 1: Поле потенциалов (MetaballField.js)

### 1.1 Преобразование модулей в источники

Каждый модуль нужно представить как набор "капель" (кругов с радиусом):

| Тип модуля | Как представить |
|------------|-----------------|
| `straight` | Цепочка кругов вдоль линии (или один "колбасный" SDF) |
| `round` | Один круг в углу + две линии |
| `bend` | Дуга = цепочка кругов вдоль дуги |
| `joint` | Пересечение = 4 линии от центра |
| `link` | Две точки (концы) |
| `central` | Крестовина = 4 линии |

**Параметры каждого источника:**
- Позиция (x, y)
- Радиус влияния (из `stem` / 2)
- Сила (обычно 1.0)

### 1.2 Формула потенциала

Для каждой точки (x, y) на сетке вычисляем сумму влияния от всех источников:

```
potential(x, y) = Σ (radius² / distance²)
```

Или более мягкий вариант:
```
potential(x, y) = Σ (1 / (1 + distance² / radius²))
```

### 1.3 Сетка дискретизации

- Создаём 2D-массив значений потенциала
- Разрешение сетки: например, 2-4 пикселя на ячейку
- Чем выше разрешение — тем плавнее контур, но медленнее

**Оптимизация:** Можно вычислять потенциал только для ячеек вблизи источников (пространственное хэширование).

---

## 🔧 Этап 2: Marching Squares (MarchingSquares.js)

### 2.1 Суть алгоритма

1. Проходим по сетке ячейка за ячейкой (2×2 вершины)
2. Для каждой вершины определяем: потенциал > порога или < порога
3. Получаем индекс конфигурации (0-15)
4. По таблице lookup определяем, какие рёбра пересекает контур
5. Интерполируем точное положение на ребре

### 2.2 Таблица конфигураций

16 возможных конфигураций для ячейки 2×2:

```
Индекс = (v0 << 0) | (v1 << 1) | (v2 << 2) | (v3 << 3)

где v0, v1, v2, v3 = 1 если потенциал > threshold, иначе 0
```

### 2.3 Выход алгоритма

- Набор отрезков (line segments)
- Их нужно соединить в замкнутые полигоны

### 2.4 Соединение отрезков в контуры

1. Берём первый отрезок
2. Ищем отрезок, начинающийся там, где заканчивается текущий
3. Продолжаем, пока не вернёмся в начало
4. Получаем замкнутый полигон

---

## 🔧 Этап 3: Сглаживание контура

### 3.1 Проблема

Marching Squares даёт "угловатый" контур (лесенка из отрезков).

### 3.2 Решение: Catmull-Rom или Bezier сглаживание

1. Взять точки контура
2. Прореживание (каждую N-ю точку) если точек слишком много
3. Применить Catmull-Rom сплайн
4. Конвертировать в кубические Bezier для SVG

### 3.3 Параметр сглаживания

- `smoothness: 0` = угловатый контур
- `smoothness: 1` = максимальное сглаживание

---

## 🔧 Этап 4: Рендеринг (MetaballRenderer.js)

### 4.1 Canvas рендеринг

```javascript
// Псевдокод
1. Собрать все модули текущего глифа
2. Преобразовать в источники потенциала
3. Вычислить поле на сетке
4. Marching Squares → контуры
5. Сгладить контуры
6. ctx.fill() или ctx.stroke()
```

### 4.2 Интеграция с ModuleDrawer

Два варианта:

**Вариант A:** Новый режим в `mode` (как `stroke`, `stripes`, `dash`, `sd`)
```javascript
mode: 'metaball'
```

**Вариант B:** Отдельный постпроцессор, который применяется после рендеринга
- Более гибкий
- Можно комбинировать с другими режимами

**Рекомендую вариант A** — чище архитектурно.

### 4.3 Параметры UI

| Параметр | Описание | Диапазон |
|----------|----------|----------|
| `threshold` | Порог изолинии (влияет на "раздутость") | 0.3 - 2.0 |
| `resolution` | Разрешение сетки | 1 - 8 px |
| `smoothness` | Степень сглаживания | 0 - 1 |
| `blobRadius` | Множитель радиуса источников | 0.5 - 2.0 |

---

## 🔧 Этап 5: Экспорт в SVG (VoidExporter.js)

### 5.1 Формат пути

Сглаженный контур → SVG path с кубическими кривыми:

```svg
<path d="M x0,y0 C c1x,c1y c2x,c2y x1,y1 C ... Z" fill="currentColor"/>
```

### 5.2 Оптимизация размера

- Округлять координаты до 2 знаков после запятой
- Использовать относительные команды (c вместо C) если это уменьшает размер

### 5.3 Множественные контуры

Если есть "дырки" (внутренние контуры), использовать `fill-rule="evenodd"`.

---

## 📋 Порядок реализации

### Фаза 1: Proof of Concept (2-3 часа)
1. ✅ Простая сетка источников (круги)
2. ✅ Базовый Marching Squares
3. ✅ Рендеринг на Canvas
4. ✅ Проверка на простом примере

### Фаза 2: Интеграция с модулями (2-3 часа)
1. ✅ Конвертация каждого типа модуля в источники
2. ✅ Учёт rotation модулей
3. ✅ Учёт stem (толщины)
4. ✅ Тестирование на реальных глифах

### Фаза 3: Сглаживание и качество (1-2 часа)
1. ✅ Catmull-Rom сглаживание
2. ⚠️ Оптимизация производительности (требует доработки)
3. ⚠️ Кэширование результатов (требует доработки)

### Фаза 4: UI и параметры (1 час)
1. ✅ Добавить вкладку/режим в интерфейс
2. ⚠️ Слайдеры для параметров (требует доработки)
3. ⚠️ Реактивное обновление (требует доработки)

### Фаза 5: SVG экспорт (1-2 часа)
1. ⚠️ Генерация path из контура
2. ⚠️ Интеграция с VoidExporter
3. ⚠️ Тестирование экспорта

---

## ⚠️ Потенциальные проблемы и решения

| Проблема | Решение |
|----------|---------|
| Медленно на большом тексте | Кэширование по глифам, Web Workers, оптимизация разрешения сетки |
| Контуры не соединяются | Проверить порог соединения точек |
| "Дырки" внутри букв | Корректная обработка внутренних контуров |
| Слишком "раздутые" формы | Уменьшить threshold или blobRadius |
| Потеря деталей | Увеличить resolution сетки |
| **Зависание браузера** | **Оптимизировать разрешение сетки, уменьшить количество источников** |
| **Не видно метаболов** | **Проверить формулу потенциала, threshold, рендеринг** |

---

## 🎨 Ожидаемый результат

- Буквы будут выглядеть как "жидкие капли"
- Соседние штрихи будут плавно сливаться
- Острые углы станут округлыми
- Общий вид — органический, биоморфный

---

## 📝 Статус реализации

- ✅ Созданы базовые файлы (MetaballField.js, MarchingSquares.js, MetaballRenderer.js)
- ✅ Интеграция в VoidRenderer
- ✅ Режим 'blob' добавлен в UI
- ⚠️ Требуется оптимизация производительности
- ⚠️ Требуется исправление рендеринга (метаболы не видны)
- ⚠️ Требуется добавление UI параметров
- ⚠️ Требуется экспорт в SVG
